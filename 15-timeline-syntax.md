# 15 — 时间轴描述语法（Timeline Syntax）

> 现有框架解决了"画面里有什么"（空间）和"怎么拍"（镜头），但缺少"什么时候发生什么"（时间）。这一章补上。

---

## 为什么需要时间轴语法

当前框架的 prompt 大多是"空间描述"——场景、构图、光影。但视频是时间媒介。一个镜头的 5 秒里，如果只说"一个女人在雨中奔跑"，模型不知道：

- 第 1 秒她在哪？第 5 秒她在哪？
- 速度是匀速还是从慢到快？
- 有没有一个情绪转折点？

空间描述决定画面质量，时间描述决定叙事质量。两者结合才是完整的视频 prompt。

---

## 时间轴三层模型

> 每一层解决一个独立问题。三层叠加 = 完整的时间叙事。

### 第一层：状态层（State Layer）

> 解决"画面从什么样变成什么样"。

显式定义三个关键帧状态，让模型知道起点和终点：

| 状态 | 含义 | Prompt 写法 |
|------|------|------------|
| **起始帧** | 第 0 秒的画面状态 | "从[具体状态]开始" |
| **过渡帧** | 中间的变化过程 | "经过[具体变化过程]" |
| **终止帧** | 最后一秒的画面状态 | "结束于[具体状态]" |

**不用时间轴的状态描述：**
```
一朵花开放。
```

**用时间轴的状态描述：**
```
一朵玫瑰从紧闭的花苞开始，花瓣缓缓向外展开，
结束于完全绽放的状态，露出深红色的内层花瓣。
```

区别在于：前者只告诉模型"要发生什么"，后者告诉模型"从哪来到哪去"。模型有了起点和终点，中间过渡帧的生成方向就明确了。

### 第二层：运动层（Motion Layer）

> 解决"怎么动、动多快、往哪动"。

运动层在状态层的基础上，定义变化的速度和轨迹：

**速度曲线：**

| 速度曲线 | 含义 | 适用场景 | Prompt 关键词 |
|----------|------|---------|--------------|
| **匀速** | 全程速度不变 | 机械运动、产品旋转 | "匀速""以恒定速度" |
| **缓入（Ease-in）** | 从静止逐渐加速 | 物体下落、起步 | "从静止开始逐渐加速" |
| **缓出（Ease-out）** | 逐渐减速到静止 | 物体停止、着陆 | "缓慢减速直到停止" |
| **缓入缓出** | 慢→快→慢 | 大多数自然运动 | "缓慢启动，加速，再缓慢停止" |
| **加速** | 越来越快 | 紧张升级、坠落 | "持续加速" |
| **突然停止** | 运动中急停 | 震惊、发现、转折 | "突然静止""急停" |

**方向向量：**

不要只说"移动"，要说移动的方向和路径：

```
❌ 镜头移动
✅ 镜头从左向右匀速平移
✅ 镜头沿弧线从人物背后绕到正面
✅ 镜头垂直上升
```

**完整运动层示例：**
```
一滴墨水从水面正中央滴入。
运动：墨水先快速下沉（缓入），在水中形成丝状扩散（匀速），
最终均匀散开直到水变成半透明灰色（缓出）。
```

### 第三层：节奏层（Rhythm Layer）

> 解决"什么时候是重点"。

节奏层用**节拍点（beat）**标记时间轴上的关键时刻，用**情绪曲线**描述整段视频的能量变化。

**节拍点标记法：**

在时间轴上标注关键事件发生的时间位置：

```
时间轴（5s）：
0s —— 起始：空旷的街道
2s —— BEAT 1：一个人影从巷口闪出
4s —— 终止：人影消失在画面另一端
```

**情绪曲线：**

用简单的能量标记描述整段视频的节奏走向：

| 曲线 | 含义 | 适用 |
|------|------|------|
| `▁▂▃▅▇` 渐进 | 从平静到高潮 | 揭示、发现、紧张升级 |
| `▇▅▃▂▁` 消退 | 从激烈到平静 | 告别、结束、释然 |
| `▂▅▁▅▂` 脉冲 | 起伏交替 | 对话、对抗、心跳 |
| `▁▁▁▁▇` 长蓄力 | 长时间平静后一个爆发 | 惊喜、转折 |
| `▇▁▁▁▁` 急停 | 开场即高潮后归于平静 | 震惊、顿悟 |

**节奏层示例：**
```
节拍编排：▁▁▁▁▇（长蓄力后爆发）

0-3s：空旷的教室，只有窗帘在动。（低能量，建立）
3-4s：一个纸飞机从画面外飞入。（能量开始上升）
4-5s：纸飞机穿过阳光，在光束中旋转。（爆发点）

声音跟随能量曲线：安静环境音 → 纸飞机划过空气的嗖声 → 短暂沉默。
```

---

## 三层叠加：完整 Prompt 示例

> 实际写 prompt 时，三层不需要分开写——它们自然融合在一段描述里。

### 示例 1：产品揭示（5s）

```
一瓶香水从黑色丝绒背景中缓慢升起（状态层：起始=画面只有黑色背景，终止=瓶子悬在画面中央）。
升起速度从静止逐渐加快再减速（运动层：缓入缓出），
瓶子最终停在画面正中，瓶身缓慢旋转 15 度。
光线：一束聚光灯从上方打亮瓶身，随瓶子升起逐渐增强（节奏层：能量渐进）。
声音：低沉的玻璃摩擦声，在瓶子停稳时消失。
约束：保持瓶身形状、标签和颜色完全不变。
```

### 示例 2：人物转身（5s）

```
一个女人背对镜头站在窗前（状态层：起始=背影）。
她缓慢转身，头发随转身惯性飘起（运动层：匀速，带惯性），
结束于四分之三侧面，视线落在窗外某处（状态层：终止=侧面轮廓）。
光线：窗户光从右侧打亮面部一半（节奏层：转身过程中光线不变）。
声音：织物摩擦的细微声，窗外远处车流。
```

### 示例 3：天气变化（10s）

```
一片麦田从晴朗午后变为暴雨（状态层：起始=金色阳光下的麦田，终止=暴雨中的麦田）。

0-3s：阳光下的麦田，麦穗在微风中轻摇。（平静）
3-5s：云层从画面左侧涌入，光线从暖色渐变为灰冷。（过渡，能量上升）
5-7s：第一滴雨落在麦穗上，溅起水花。（BEAT 1：雨开始）
7-10s：暴雨倾盆，麦穗在风雨中大幅摇摆。（高潮）

运动层：云层匀速推进；雨滴从稀疏到密集（加速）；麦穗摇摆幅度从小到大。
节奏层：▂▃▅▇▇（持续升级）。
声音：微风沙沙 → 远处雷声 → 雨打麦叶 → 暴雨轰鸣。
```

### 示例 4：食物特写（5s）

```
一块牛排放在铸铁锅上（状态层：起始=生肉表面粉红）。
表面从粉红变为焦褐色，油脂从边缘冒出细密气泡（运动层：渐变，匀速）。
蒸汽从肉表面升起，在画面上方形成薄雾（节奏层：持续渐进）。
结束于表面形成完整的焦壳，油脂在边缘微微冒烟。
光线：从锅的侧面打暖色光，全程保持不变。
声音：油脂滋滋声从小到大。
```

### 示例 5：城市延时（10s）

```
城市十字路口从深夜到清晨（状态层：起始=空无一人的深夜路口，终止=早高峰人流）。

0-4s：空旷路口，只有红绿灯在变。（低能量，▁▁）
4-6s：第一辆车驶入路口，车灯在湿路面上拖出光带。（BEAT 1，▂）
6-8s：更多车辆和行人出现，速度逐渐加快。（能量上升，▃▅）
8-10s：路口完全填满车流和人流，节奏正常。（▇）

运动层：车辆从稀疏到密集（加速曲线）；行人从零星到成群。
光线：从深蓝色夜空渐变为灰白晨光（色温从冷到中性）。
节奏层：▁▁▂▃▅▇（长蓄力，渐进填充）。
声音：安静 → 一辆车经过 → 多辆车 → 城市白噪音。
```

---

## 时间轴语法速查

### 状态层关键词

| 中文 | 用途 | 示例 |
|------|------|------|
| 从…开始 | 定义起始帧 | "从紧闭的花苞开始" |
| 经过 | 定义过渡过程 | "花瓣缓缓向外展开" |
| 结束于 / 停在 | 定义终止帧 | "结束于完全绽放" |
| 保持…不变 | 约束不变元素 | "保持瓶身形状不变" |

### 运动层关键词

| 中文 | 速度曲线 | 适用 |
|------|----------|------|
| 匀速 | 恒定 | 机械运动、产品旋转 |
| 从静止开始逐渐加速 | 缓入 | 起步、下落 |
| 缓慢减速直到停止 | 缓出 | 着陆、停止 |
| 缓慢启动，加速，再缓慢停止 | 缓入缓出 | 自然运动（默认选择） |
| 持续加速 | 加速 | 紧张升级 |
| 突然静止 / 急停 | 急停 | 震惊、转折 |

### 节奏层关键词

| 中文 | 能量曲线 | 适用 |
|------|----------|------|
| 从安静到激烈 | 渐进 | 揭示、升级 |
| 从激烈到安静 | 消退 | 告别、释然 |
| 起伏交替 | 脉冲 | 对话、对抗 |
| 长时间平静后一个爆发 | 长蓄力 | 惊喜、转折 |
| 开场即高潮后归于平静 | 急停 | 震惊、顿悟 |

---

## 与现有框架的关系

时间轴三层模型不是独立系统，是对现有框架的补充：

| 现有框架 | 解决的问题 | 时间轴补充 |
|----------|-----------|-----------|
| 导演五问（01） | 这场戏做什么 | 节奏层定义"什么时候做" |
| 镜头合约（02） | 怎么拍 | 运动层定义镜头怎么动、动多久 |
| 节拍编排（01 Step 7） | 能量模式 | 时间轴三层是节拍编排的微观版 |
| 反空话（09） | 不说空话 | 时间轴要求每个状态变化可观察 |

如果你已经熟悉节拍编排（Step 7），时间轴三层模型就是它的微观版——节拍编排管"镜头之间的节奏"，时间轴三层管"一个镜头内部的节奏"。

> 节奏层的音频设计怎么写？[08-音频指南](08-audio-guide.md) 覆盖了音画同步的三层模型（物理层/情绪层/叙事层）和 10 个场景的音画配对示例。

---

## 检查清单

写完 prompt 后，检查时间轴：

- [ ] 有明确的起始状态？（读者知道第 0 秒画面长什么样）
- [ ] 有明确的终止状态？（读者知道最后一秒画面长什么样）
- [ ] 运动有速度描述？（匀速/缓入/缓出/加速，而不是只说"动"）
- [ ] 运动有方向描述？（从左到右/从下到上/弧线）
- [ ] 至少一个节拍点？（知道最重要的那一刻在哪）
- [ ] 声音跟随能量变化？（不是全程一个音量）
