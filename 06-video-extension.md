# 06 — 视频续写（Video Extension）

> 续写的基础是已接受的素材或已接受的最终帧。不要从计划状态出发，要从观察到的状态出发。

---

## 接续门控

写续写 prompt 之前，必须知道：

1. **已接受的源素材或最终帧** — 观察到的结束状态
2. **下一片段的意图** — 观众应该感受到什么
3. **已完成的节拍** — 不能重复
4. **预留的未来节拍** — 不能提前展示
5. **连续性锁定** — 服装、道具、光线方向、位置

如果源素材缺失，先要求提供。不要编造。

---

## 观察快速路径

- **最终帧已提供：** 直接从画面读取——姿势、画面位置、服装道具、环境、光线阶段、构图。只追问运动方向、镜头运动阶段、音频阶段。
- **完整片段已提供：** 读取一切，包括运动、镜头阶段、音频阶段。
- **什么都没提供：** 才要求用户描述可见的结束状态。

---

## 接续类型

| 类型 | 何时用 | 标记 |
|------|--------|------|
| **无缝续接** | 下一帧继续同一镜头、同一空间、同一开放运动 | `从上一片段最后一帧无缝续接` |
| **有意下一镜** | 场景切换，需要编辑性剪辑 | `有意的新镜头` |
| **桥接** | 已知起点状态必须到达已知终点状态 | `桥接两个已知状态` |
| **修复尾巴** | 上一片段最后几秒失败了 | `修复结尾` |
| **重新锚定** | 续写深度或可见漂移让链条不稳定 | `重新锚定` |

---

## 无缝续接 Prompt 模板

```
从上一片段的最后一帧续接。
保持角色、服装、光线方向和镜头角度不变。
继续唯一的动作：[具体动作]。
镜头保持[锁定/同一运动]。光线和房间布局保持连续。
约束：无新场景、无新镜头运动、无额外特效。
```

### 示例

```
从上一片段的最后一帧续接。保持角色、服装、光线方向和镜头角度不变。
烟雾在门口散开，角色放下灯笼。
镜头保持锁定中景。光线和房间布局保持连续。
约束：无新场景、无新镜头运动、无额外特效。
```

---

## 漂移修复

当续写链条出现身份漂移（面部变形、服装变化、环境错位）：

1. **回到原始参考** — 用最初的图片参考，不用上一片段的输出
2. **缩短片段** — 每个续写片段控制在 5 秒以内
3. **减少复杂度** — 只延续一个动作，不加新元素
4. **声明约束** — 明确列出不能变的东西

```
重新锚定。[图片 1] 重新锁定角色身份。
保持面部结构、发型、服装完全一致。
只继续一个动作：角色缓慢转身。
镜头锁定。声音保持连续。
```

---

## 已完成/预留节拍规则

每个续写 prompt 必须：
- **排除** 已完成的节拍（不要重复）
- **排除** 预留的未来节拍（不要提前）
- **只做** 当前片段的事

**示例：**
- 片段 01 已经出了地铁站 → 片段 02 不能再出地铁站
- 车辆出发预留给片段 03 → 片段 02 必须在出发前停下

---

## 序列状态记录

每完成一个片段，记录：

| 字段 | 内容 |
|------|------|
| 可见结束状态 | 姿势、画面位置、环境 |
| 开放运动方向 | 角色向哪个方向移动 |
| 镜头阶段 | 镜头在推进中/已锁定/正在摇 |
| 光线阶段 | 自然光角度、人造光源状态 |
| 声音阶段 | 环境音、音乐、对话 |
| 连续性锁定 | 服装、道具、位置 |
| 已完成节拍 | 列表 |
| 预留节拍 | 列表 |

---

## 视频编辑操作（Video Editing）

> 借鉴 Seedance 2.0 的视频编辑公式。核心：用参考视频+参考图片对已有视频做局部修改。

### 三种编辑操作

| 操作 | Prompt 公式 | 用途 |
------|-------------|------|
| **添加** | 在 [Video N] 的 [时间点] 添加 [元素] | 加特效、加人物、加道具 |
| **移除** | 移除 [Video N] 中的 [元素]，其余保持不变 | 去水印、去人物、去物体 |
| **替换** | 将 [Video N] 中的 [元素 A] 替换为 [Image 1] 中的 [元素 B] | 换产品、换背景、换服装 |

### 添加示例

```
[Video 1] 为原始视频。
在视频的第 3 秒，画面右侧添加一只飞过的白鸽。
保持原始镜头运动、光线和人物动作不变。
约束：不改变原有内容，只增加新元素。
```

### 替换示例

```
[Video 1] 为原始视频。[Image 1] 为新产品参考。
将视频中的香水瓶替换为 [Image 1] 中的面霜，
保持所有原始动作、镜头运动和光线不变。
约束：无新文字、无水印、无身份变化。
```

### 关键规则

1. **编辑是局部的** — 只改变声明的部分，其余必须保持不变
2. **参考图锁定替换目标** — 用 [Image N] 提供替换后的外观
3. **声明排除项** — 明确列出不能变的元素

---

## 视频续写公式（Video Extension Formulas）

> 借鉴 Seedance 2.0 的结构化续写公式。比无缝续接模板更灵活。

### 三种续写模式

| 模式 | Prompt 公式 | 用途 |
------|-------------|------|
| **向前续写** | Generate content after [Video N]: [描述] | 从结尾继续 |
| **向后续写** | Extend the opening of [Video N]: [描述] | 补充开头 |
| **拼接** | [Video 1] + [过渡] + followed by [Video 2] | 多段拼接 |

### 向前续写示例

```
[Video 1] 为上一片段。
从 [Video 1] 的最后一帧继续：角色转身离开，
镜头缓慢后退，揭示整个房间的全貌。
保持角色、服装和光线方向不变。
```

### 拼接示例

```
[Video 1] 为第一段，[Video 2] 为第二段。
[Video 1] 的结尾通过淡出过渡到 [Video 2] 的开头。
保持两个片段的角色身份和色调一致。
约束：过渡自然，无跳切。
```

### 多参考视频混合

```
[Video 1] 为第一段（室内），[Video 2] 为第二段（室外），[Video 3] 为镜头运动参考。
[Video 1] 通过走廊过渡到 [Video 2] 的户外场景。
镜头运动参照 [Video 3] 的节奏。
保持角色身份和服装在两个片段间一致。
```

续写 prompt 发出前，验证：

- [ ] 前置片段已接受
- [ ] 观察到的结束状态已记录
- [ ] 已完成节拍已排除
- [ ] 未来节拍已排除
- [ ] 参考标签字节级一致
- [ ] 只覆盖当前片段
- [ ] 意图明确且被可见选择服务
