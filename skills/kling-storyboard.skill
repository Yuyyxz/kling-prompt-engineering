# 分镜表输出规范

> 可灵AI分镜表输出模板，用于从剧本自动生成可执行的分镜表。

---

## 九列分镜表格式

| 列名 | 说明 | 示例 |
|------|------|------|
| **镜头编号** | 场景-镜头号 | S1-01 |
| **景别** | 极远景/远景/中景/中近景/近景/极近景/微距 | 中近景 |
| **角度** | 平视/低角度/高角度/俯拍/侧面/过肩 | 平视 |
| **运动** | 锁定/推进/后退/横移/环绕/跟拍/手持/无人机 | 缓慢推进 |
| **主体** | 画面中的主要元素 | 角色A坐在桌前 |
| **动作** | 主体的可见动作 | 低头看手机 |
| **光线** | 光源/方向/色温 | 侧光，暖色调 |
| **声音** | 环境音/对话/音乐 | 键盘声、空调声 |
| **时长** | 建议秒数 | 5s |
| **提示词** | 完整的可灵提示词 | [完整提示词] |

---

## 输出示例

### 产品广告（15秒）

| 镜头 | 景别 | 角度 | 运动 | 主体 | 动作 | 光线 | 声音 | 时长 | 提示词 |
|------|------|------|------|------|------|------|------|------|--------|
| S1-01 | 近景 | 平视 | 锁定 | 产品放在桌面 | 冷凝水珠滑落 | 侧光，冷色调 | 水滴声 | 3s | 产品放在大理石桌面上，冷凝水珠缓缓滑落。镜头：近景，锁定。光线：侧光，冷色调。声音：水滴声。约束：无文字、保持logo不变。 |
| S1-02 | 微距 | 俯拍 | 缓慢推进 | 产品logo | 光线扫过logo | 顶部光，暖色 | 无 | 3s | 产品logo特写，光线缓缓扫过。镜头：微距，俯拍，缓慢推进。光线：顶部光，暖色调。约束：保持logo完全不变。 |
| S1-03 | 中景 | 平视 | 环绕 | 产品全貌 | 蒸汽升起 | 侧光，暖色 | 蒸汽声 | 4s | 产品全貌，蒸汽缓缓升起。镜头：中景，缓慢环绕。光线：侧光，暖色调。声音：蒸汽声。约束：无文字。 |
| S1-04 | 近景 | 平视 | 锁定 | 产品+背景 | 品牌logo出现 | 逆光 | 品牌音效 | 5s | 产品在逆光中，品牌logo出现。镜头：近景，锁定。光线：逆光。约束：保持logo不变。 |

---

## 分镜表生成流程

### 从剧本到分镜表

```
剧本 → 场景拆解 → 镜头设计 → 提示词生成 → 分镜表
```

1. **场景拆解**：把剧本按场景分割
2. **镜头设计**：每个场景拆成2-5个镜头
3. **提示词生成**：每个镜头生成完整提示词
4. **分镜表输出**：整理成九列表格

### 镜头设计原则

1. **一个镜头 = 一个变化**——不要在一个镜头里塞太多动作
2. **开头用Hook**——第一个镜头必须吸引注意力
3. **结尾用收束**——最后一个镜头要有明确的结束感
4. **中间有节奏**——快慢交替，动静结合

---

## Python 生成脚本

```python
import json

def generate_storyboard(scenes):
    """从场景列表生成分镜表"""
    storyboard = []
    shot_num = 1
    
    for scene in scenes:
        scene_id = scene['id']
        for shot in scene['shots']:
            storyboard.append({
                'shot_id': f'S{scene_id}-{shot_num:02d}',
                'shot_size': shot['size'],
                'angle': shot['angle'],
                'movement': shot['movement'],
                'subject': shot['subject'],
                'action': shot['action'],
                'lighting': shot['lighting'],
                'sound': shot['sound'],
                'duration': shot['duration'],
                'prompt': shot['prompt']
            })
            shot_num += 1
    
    return storyboard

def export_to_csv(storyboard, filename):
    """导出为CSV"""
    import csv
    with open(filename, 'w', newline='', encoding='utf-8') as f:
        writer = csv.DictWriter(f, fieldnames=[
            'shot_id', 'shot_size', 'angle', 'movement',
            'subject', 'action', 'lighting', 'sound',
            'duration', 'prompt'
        ])
        writer.writeheader()
        writer.writerows(storyboard)

# 示例
scenes = [
    {
        'id': 1,
        'shots': [
            {
                'size': '近景',
                'angle': '平视',
                'movement': '锁定',
                'subject': '产品放在桌面',
                'action': '冷凝水珠滑落',
                'lighting': '侧光，冷色调',
                'sound': '水滴声',
                'duration': '3s',
                'prompt': '产品放在大理石桌面上，冷凝水珠缓缓滑落。镜头：近景，锁定。光线：侧光，冷色调。声音：水滴声。约束：无文字、保持logo不变。'
            }
        ]
    }
]

storyboard = generate_storyboard(scenes)
export_to_csv(storyboard, 'storyboard.csv')
```

---

## 注意事项

1. **提示词必须完整**——每个镜头的提示词可以直接复制到可灵使用
2. **脆弱锚点必须声明**——面部、logo、文字、手部
3. **时长要合理**——5s只能有一个动作，15s可以有3个节拍
4. **声音要标注**——可灵支持原生音频，声音是重要组成部分
5. **约束要明确**——无文字、无水印、保持XX不变
