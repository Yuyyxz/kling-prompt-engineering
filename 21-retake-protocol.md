# 21 — 重试协议：生成之后怎么办

> 生成按钮按下去只是半程。回来那一坨东西到底留不留、改不改、还是从头来——这个决定比写 prompt 更烧钱。

---

## 为什么需要重试协议

可灵一次生成 5 秒 4K 视频要 15 积分，10 秒 30 积分。没有协议的重试就是：看一眼觉得"差不多"→改两个参数→再生成→还是差不多→再改→再花。一个 5 秒镜头烧掉 100 积分的事天天有。

重试协议不是让你少花钱。是让你**知道什么时候该停**。

---

## 每次生成回来，给 5 个判定

拿到结果别急着看细节。先回答一个问题：**这趟的核心交付拿到了没有？**

| 判定 | 什么时候用 | 下一步 |
|------|-----------|--------|
| **留** | 核心交付到位，没有致命硬伤 | 锁死，记日志，往下走。次要瑕疵是后期的活 |
| **后期修** | 问题是色彩、文字、混音、片头片尾几帧不稳 | 别烧生成次数去修后期 5 分钟能搞定的事 |
| **编辑不动** | 构图和节奏对了，只有一层不对，且参考图/视频支持局部编辑 | 保留这趟作为源素材，只改出错的那层 |
| **重新采样** | prompt 没问题，纯粹是运气差（采样随机性） | 同一个 prompt 跑新的。最多 2-3 次——超过 3 次就是 prompt 的问题 |
| **重写 prompt** | 同一个毛病在 2 趟以上重复出现 | 这是系统性问题，不是运气。诊断原因，改 prompt |

关键判断：**同一个毛病出现两次 = 重写 prompt，不是再跑一趟。** 这是最容易犯的错——觉得"下一趟可能就好了"。不会好的。

---

## 单变量规则

每次重试只改一个东西。

改一个 prompt 从句，或者换种子，或者切时长，或者换一张参考图——但只动一个。

同一个种子 + 改一个从句 = 你能做的最接近"控制变量实验"的事。
新种子 + 同一个 prompt = 纯重新采样。
同时改两个东西 = 不管结果好坏你都不知道为什么。白跑。

---

## 重试预算

**在第一趟生成之前就定好。** 不是第三趟花完了再想。

定两件事：
1. **最多跑几趟**（建议：普通质量 5 趟，草稿 10 趟）
2. **"够用"长什么样**（核心交付到位 + 次要瑕疵可后期修）

预算花了一半、同一个毛病没进展——停。换策略：换模型参数、拆成更多镜头、或者看下面的"诚实退出"。

没有止损线的迭代，就是一个 5 积分的镜头变成 50 积分的镜头的过程。

---

## 拍摄日志

每趟一行。这是你的审计线索：

```
#01 · 改: 无（首趟） · 判定: 重新采样 · 证据: 手指穿模
#02 · 改: 种子 · 判定: 重新采样 · 证据: 手指仍穿模
#03 · 改: prompt 加"双手静止放在桌面" · 判定: 留 · 证据: 手部正常
```

日志比记忆靠谱。翻回去看两行就知道：同一个毛病出现两次 = 该重写 prompt 了。

---

## 诚实退出：什么时候该放弃生成

有些东西不该在生成里死磕：

- **密集文字** → 后期叠加，别指望模型渲染
- **真实产品的精确行为** → 实拍可能更便宜
- **连续两次超预算还没修好** → 拆镜头、换思路、或者承认这个镜头不适合当前工具
- **模型物理上做不到** → 精确的手指交互、复杂的多物体碰撞、超过 10 秒的连贯叙事

"这镜头实拍"是一个交付决定，不是失败。

---

## 实战场景

### 场景 A：产品广告重试

你在做一个香水瓶的冷凝水镜头。

```
#01 · 改: 无（首趟） · 判定: 重新采样 · 证据: 瓶身 logo 变形
#02 · 改: 种子 · 判定: 重新采样 · 证据: logo 仍变形
```

到 #02 你就该停了——logo 变形重复出现，这是系统性问题。
重写 prompt：把"保持 logo 完全不变"提到第一句，加"约束：标签文字、形状、颜色零变化"。

```
#03 · 改: prompt 前置身份约束 · 判定: 留 · 证据: logo 正常，冷凝水稍弱
```

冷凝水弱？后期能加。logo 正常 = 核心交付到位。留。

### 场景 B：人物表演重试

你在做一个"夜班前台看到监控里的失踪弟弟"的镜头。

```
#01 · 改: 无（首趟） · 判定: 重新采样 · 证据: 表情太平，没有内心波动
#02 · 改: 种子 · 判定: 重新采样 · 证据: 仍然太平
```

两次都太平。不是运气——是 prompt 没有给演员一个具体的身体动作来承载内心冲突。

重写 prompt：把"表情复杂"删掉，换成一个可见的抑制行为——"她的手指悬在回放键上方，然后缩回来，扶正了桌上歪掉的 214 号房钥匙牌"。

```
#03 · 改: prompt 用具体行为替代情绪形容 · 判定: 留 · 证据: 手部动作到位，背景稍糊
```

背景糊 = 长焦景深效果，反而是对的。留。

### 场景 C：动作场景重试

你在做一个"外卖员雨中骑车穿过窄巷"的跟随镜头。

```
#01 · 改: 无（首趟） · 判定: 重新采样 · 证据: 电动车轮子变形
#02 · 改: 种子 · 判定: 重新采样 · 证据: 轮子仍变形
#03 · 改: 种子 · 判定: 重新采样 · 证据: 轮子好了一点但车身晃
```

三趟了，车辆形态不稳定。这是快速运动 + 复杂几何的老问题。

换策略：把 10 秒拆成两个 5 秒。第一段只拍"骑车进入巷子"（慢速），第二段拍"穿过积水的特写车轮"（微距遮住了整体形态问题）。

```
#04 · 改: 拆成两段，第一段降速 · 判定: 留 · 证据: 车身正常
#05 · 改: 第二段微距车轮 · 判定: 留 · 证据: 水花到位，形态稳定
```

拆镜头比死磕一个镜头便宜。
